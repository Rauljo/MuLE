# Execution

Execute run_generation.sh with the following parameters:

- GPU_ID
- BASE_DIR
- FAKE_HOME = where to save all libraries data
- INPUT_FILE = the questions to answer
- OUTPUT_FILE = where to save the answers

If the execution is stopped, once run again the execution would start from the last question not answered. 
